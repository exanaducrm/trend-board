# 다른 사람도 보게 하기

두 가지 방법이 있습니다. 목적이 다르니 상황에 맞는 쪽을 고르세요.

| | 사내망 공유 | 깃허브 페이지 |
|---|---|---|
| 접속 범위 | 같은 네트워크 안에서만 | 인터넷 어디서나 |
| 필요한 것 | 이 PC를 켜 둘 것 | 깃허브 계정 |
| 데이터 공개 | 안 됨 | 누구나 볼 수 있음 |
| 기간 버튼 | 됨 | 됨 (미리 받아둔 자료에서 고름) |
| 차단 위험 | 낮음 | 높음 (아래 참고) |

---

## 방법 1. 사내망 공유

**`share.bat` 을 실행하면 끝입니다.**

이 PC의 주소가 화면에 뜹니다. 그중 하나를 다른 PC 브라우저에 입력하면 됩니다.

```
이 PC의 주소
  http://192.168.0.15:8787
```

처음 실행하면 윈도우 방화벽이 물어봅니다. **개인 네트워크 허용**을 눌러 주세요.
안 뜨는데 접속이 안 되면 방화벽에서 파이썬을 수동으로 허용해야 합니다.

이 PC를 끄면 보드도 꺼집니다. 계속 띄워둬야 한다면 절전 모드를 꺼 두세요.

---

## 방법 2. 깃허브 페이지

깃허브 서버가 20분마다 대신 수집해서 JSON을 만들고,
페이지는 그 JSON을 읽는 화면만 서비스합니다. 파이썬은 깃허브에서만 돕니다.

화면에 보이는 "다음 수집까지"는 마지막 수집 시각에 주기를 더한 값이라 **예정**입니다.
깃허브가 늦게 돌면 그만큼 어긋납니다. 그래서 뒤에 "예정"이라고 붙여 둡니다.

`cron` 값을 바꾸면 `--every` 값도 같이 맞춰야 숫자가 맞습니다.

### 먼저 알아둘 것

**쇼핑몰이 막을 수 있습니다.** 깃허브 서버는 데이터센터 IP를 씁니다.
일반 가정이나 사무실 IP와 달라서 봇으로 판단해 차단하는 곳이 많습니다.
G마켓이 이미 그렇게 막았고, 다른 곳도 같은 판단을 할 수 있습니다.
돌려보고 실패하는 소스가 있으면 그건 코드 문제가 아니라 차단입니다.

**자료가 공개됩니다.** 저장소를 비공개로 해도 깃허브 페이지 주소는 누구나 열 수 있습니다.
사내용으로만 쓸 자료라면 방법 1이 맞습니다.

**주기가 정확하지 않습니다.** 20분으로 적어도 깃허브 사정에 따라 몇 분에서
수십 분 늦게 돌 수 있습니다. 실시간이라고 보기는 어렵습니다.

**한 번 도는 데 1분 안팎 걸립니다.** 데이터랩 두 소스를 일간·주간·월간 세 번씩 받기 때문입니다.
분야 11개 × 2소스 × 3기간이라 요청이 66번 나갑니다. 간격을 두고 보내므로 시간이 걸립니다.

**60일 동안 저장소에 변경이 없으면 예약 실행이 멈춥니다.** 그때는 Actions 탭에서
한 번 눌러 다시 켜야 합니다.

### 순서

**1. 저장소 만들기**

깃허브에서 새 저장소를 만들고, 이 폴더의 파일을 올립니다.
`.github/workflows/collect.yml` 도 같은 구조로 올라가야 합니다.

```
git init
git add .
git commit -m "쇼핑 트렌드"
git branch -M main
git remote add origin https://github.com/사용자이름/저장소이름.git
git push -u origin main
```

`state.json` 은 올리지 마세요. `.gitignore` 에 이미 넣어 두었습니다.

**2. 페이지 켜기**

저장소 **Settings → Pages → Build and deployment → Source** 를
**GitHub Actions** 로 바꿉니다.

**3. 한 번 돌려보기**

**Actions** 탭 → 왼쪽에서 **쇼핑 트렌드 수집과 배포** → **Run workflow**.
2~3분 뒤 초록색으로 끝나면 됩니다.

로그의 `수집해서 JSON 만들기` 단계를 열면 소스별로 몇 건 받았는지 나옵니다.
전부 실패하면 그 단계가 빨간색이 되므로 바로 알 수 있습니다.

**4. 주소 확인**

Settings → Pages 위쪽에 주소가 뜹니다.

```
https://사용자이름.github.io/저장소이름/
```

### 주기 바꾸기

`.github/workflows/collect.yml` 의 이 줄을 고칩니다.

```yaml
- cron: "*/20 * * * *"     # 20분마다
- cron: "0 * * * *"        # 매시 정각
- cron: "0 0,6,12,18 * * *"  # 하루 네 번 (UTC 기준)
```

`cron` 은 UTC 기준입니다. 한국 시간보다 9시간 빠릅니다.
자주 돌릴수록 차단당할 가능성이 올라가니 필요 이상으로 짧게 잡지 마세요.

---

## 올리기 전에 미리 보기

깃허브 페이지에서 어떻게 보일지 내 PC에서 확인할 수 있습니다.

**`preview.bat` 을 실행하세요.** 한 번 수집해 `snapshot.json` 을 만들고,
그 파일만으로 화면을 띄웁니다. 몇 개 소스가 살아남는지 여기서 판단하시면 됩니다.

일간·주간·월간은 세 가지를 미리 다 받아 파일에 담아두기 때문에 버튼이 그대로 동작합니다.
누르면 다시 수집하는 게 아니라 이미 받아둔 자료에서 골라 보여줍니다.

### dashboard.html 을 직접 열면 안 됩니다

파일을 두 번 눌러 브라우저로 여는 방식(`file://`)은 동작하지 않습니다.
그렇게 연 페이지는 브라우저가 같은 폴더의 파일도 못 읽게 막습니다.
반드시 `preview.bat` 이나 `run.bat` 을 거쳐야 합니다.

명령으로 직접 하시려면,

```
python server.py --export snapshot.json
python -m http.server 8000
```

그리고 http://127.0.0.1:8000/dashboard.html 로 들어가시면 됩니다.
